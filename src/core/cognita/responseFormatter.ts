import { Mode } from './systemPrompt'

export function formatResponse(text: string, mode: Mode): string {
  // Placeholder: format response based on mode
  // This should be replaced with actual implementation from the original monorepo
  return text
}

